server:
  port: 8081
spring:
  application:
    name: demo-api
  profiles:
    active: dev
  jmx:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS
logging:
  file:
    name: logs/archetype-api.log
